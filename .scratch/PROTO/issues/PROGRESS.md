# PROGRESS — live build status (dotden v1)

The single source of truth for **what is done / in flight / blocked**. Agents update their issue's row
(and append a line to the Log) when they **start** and **finish**.

## How to update

- **Starting:** set Status `🟡 In progress`, put your agent id + date in *Who* / *Started*.
- **Finishing:** set Status `✅ Done`, fill *Finished* + *Branch/PR* + a one-line *Outcome*; append a **Log** entry.
- **Blocked:** set `⛔ Blocked`, note the reason + blocking issue in *Outcome*.
- An issue is **grabbable** only when every code in its *Blocked by* (see [TRACK.md](./TRACK.md)) is `✅ Done`.

Status key: `⬜ Not started` · `🟡 In progress` · `🔵 In review` · `✅ Done` · `⛔ Blocked`

## Ready now (no blockers)

- `2-01` — [issue-2-01-history-tab.md](./issue-2-01-history-tab.md) _(unblocked by 1-01 + 1-07)_
- `3-01` — [issue-3-01-commit-wide-event-ring-buffer.md](./issue-3-01-commit-wide-event-ring-buffer.md) _(unblocked by 1-04)_
- `3-23` — [issue-3-23-license-choice.md](./issue-3-23-license-choice.md)

> Everything else unlocks as its blockers reach ✅. PRD 2 work generally needs the PRD 1 thread done; PRD 3 needs PRD 1 (+ some PRD 2) done.

## PRD 1 — MVP (the sync loop)

| Issue | Title | Type | Blocked by | Status | Who | Started | Finished | Branch/PR | Outcome |
|-------|-------|------|-----------|--------|-----|---------|----------|-----------|---------|
| `1-00` | Spike: validate @pierre/trees renderRowDecoration two-axis row geometry | HITL | — | ✅ Done | claude | 2026-06-15 | 2026-06-15 | .scratch/proto-row-decoration | GO — keep the overlay. beta.4 draws a coloured M/A/D/R/U letter; `renderRowDecoration` text lands directly left of it (`↓ M`/`⚠ U`), no overlap/clip at compact/default/relaxed. No `unsafeCSS`, no `setGitStatus` fallback. Recipe + shots in issue-00. |
| `1-01` | Faithful-wrapper foundation — ChezmoiAdapter/GitTransport + Vitest harness | AFK | — | ✅ Done | pi | 2026-06-15 | 2026-06-15 | workspace | Added Vitest harness, temp git fixture, ChezmoiAdapter/GitTransport, bundled-tool resolver, ADR 0019, and real chezmoi/git integration tests. |
| `1-02` | ~~Register GitHub App~~ — DEFERRED to convenience layer (V1-Lean, ADR 0020) | HITL | — | ⏸️ Deferred | | | | | |
| `1-03` | RemoteClient — connect existing Remote, ls-remote preflight, system git creds | AFK | — | ✅ Done | pi | 2026-06-15 | 2026-06-15 | workspace | Added RemoteClient, timeout/cancel process support, typed IPC/preload bridge, minimal Connect URL UI, and tests for preflight/init/latest SHA/no env override/sanitized diagnostics. |
| `1-04` | First end-to-end thread — Track → Commit → Sync → second-env Apply (incoming-clean) | AFK | 1-01, 1-03 | ✅ Done | workflow | 2026-06-15 | 2026-06-15 | feat/main-app | End-to-end thread real across two envs: IpcBridge (_trace on every call) + OperationTracer (allowlisted wide events, bounded ring) + SyncEngine (incoming-clean route) + ApplicabilityResolver (un-forgeable AppliesHere) + ApplyPlanner + MyenvStore (.myenv/) + 3-pane shell using @pierre/trees+@pierre/diffs. 51 tests incl. SyncEngine property + real-binary e2e. |
| `1-05` | Environment registry & identity — stable id, editable label, git-log attribution | AFK | 1-04 | ✅ Done | workflow | 2026-06-15 | 2026-06-15 | feat/main-app | EnvironmentRegistry: stable id (UUID, hostname-independent) + editable label (one-line registry diff, no churn) + attribution derived live from `git log` (never persisted). Own id mirrored to local chezmoi `[data].dotden_env_id` (spike-proven subscription-`.chezmoiignore` seam); new-or-returning probe + claim adopt id; suggestClaims by OS+setup-hostname (no auto-merge). `env:*` IPC + editable EnvironmentBadge UI. 69 tests green. |
| `1-06` | Tool-catalog discovery scan + first-environment onboarding | AFK | 1-04 | ✅ Done | workflow | 2026-06-15 | 2026-06-15 | feat/main-app | DiscoveryScanner (catalog-grounded home-dir scan, feature-detection not sweep, ADR 0022) + `discover:*` IPC; OnboardingShell + OnboardingMenu rail + OBContent steps Welcome/CreateRepo/ConnectURL(reuses 1-03 preflight)/Discover/Commit/AutoSync(wired slot)/Done; drag-in/browse for missed Files; picks Track via 1-04 path → default Workspace auto-seeded; App routes onboarding↔Workspace. 81 tests green. |
| `1-07` | Three-pane view — tree interactions, git-status decorations, inspector, diff | AFK | 1-04 | ✅ Done | workflow | 2026-06-15 | 2026-06-15 | feat/main-app | Real three-pane wired chezmoi↔main↔IPC↔UI: `den:tree`/`den:diff` IPC over `DenService.fileTree`/`fileDiff` (managed + parsed `chezmoi status` local axis M/A/D from **column X only** — col Y is the incoming axis owned by 1-09 — + `chezmoi ignored` muted + `.myenv/` placement; per-File `chezmoi diff`). Renderer @pierre/trees with setGitStatus + search + inline rename + drag-reorganize + renderRowDecoration seam (1-09); @pierre/diffs PatchDiff of selected File; inspector FILE info. @pierre/trees beta pinned + pnpm-patched (Math.random sticky-jitter→0); Shiki trimmed to config langs via a grammar-free `shiki` shim (308→93 chunks). gpgsign-off test hygiene. 91 tests green. |
| `1-08` | Right-click row actions + Untrack + Delete everywhere | AFK | 1-07 | ✅ Done | workflow | 2026-06-15 | 2026-06-15 | feat/main-app | Right-click row menu (`@base-ui` ContextMenu over `@pierre/trees`, target via `data-item-path`) offers Commit · Apply · Untrack · Delete everywhere (latter visibly distinct: separator + red). Untrack→`forget` (File STAYS on disk everywhere, Default-tone confirm says so); Delete everywhere→`destroy` (Destructive-tone confirm NAMES every subscribed environment via `den:affected-environments`). Both commit LOCALLY (ADR 0006). **Caught+fixed a real bug**: forget/destroy *deletes* the source-state file but the commit only staged `.myenv/`, leaving the deletion unstaged → it would re-appear on Sync; switched to `commitAll` so the removal travels (proven by a cross-env clone test). +4 DenService e2e tests; 95 tests green. |
| `1-09` | Remote-axis decorations + Review & Apply surface (one/all, atomicity, retry) | AFK | 1-07, 1-00 | ✅ Done | workflow | 2026-06-16 | 2026-06-16 | feat/main-app | Two-axis rows (Remote ↓/⚠ via `renderRowDecoration`, spike geometry) + top-level "N incoming from `<env>`" banner + dedicated Review & Apply surface (grouped CONFLICTS/APPLIES-CLEANLY, incoming diff before Apply, Apply one/all). `applyIncoming` rewritten **per-file atomic** (one `chezmoi apply` per File → one failure never blocks the rest), reports each failure with a reason + retry that re-runs ONLY the failures. New `den:incoming-summary`/`den:incoming-diff` IPC. 114 tests green (+8: per-file atomicity unit + retry + non-applicable; e2e summary/diff/marker; remote-axis mapping). |
| `1-10` | ApplyPlanner invariants — uncommitted-edit guard + incoming-deletion confirmation | AFK | 1-09 | ✅ Done | workflow | 2026-06-16 | 2026-06-16 | feat/main-app | `ApplyPlanner` made SOLE owner of invariants #2 (never-lose-data-silently) + #4 (confirm-incoming-deletions): `planIncoming(changes, localEdits)` tags each item `blockedReason:'uncommitted-edit'` (data-driven, no special case) + `requiresConfirmation` for every `delete`; `ready`/`blocked`/`deletions` splits so no caller re-classifies. **Authoritative atomic re-check** lives in `ChezmoiAdapter.applyGuarded` (status-then-write, no plan→apply TOCTOU) → `UncommittedLocalEditError`; `localEdits()` reads `chezmoi status` col X. SyncEngine gains the `incoming-delete` event path (+ `localEdits` passthrough), consuming the planner verdict—never re-checking. `DenService.applyIncoming(paths, trace, confirmedDeletions)` consumes blocks/confirmations + uses the guard; `listIncomingClean`/`IncomingReviewItem` carry kind+requiresConfirmation; `apply` IPC gains `confirmedDeletions`. **Review fix (2026-06-16)**: invariant #4 was NOT actually enforced on Apply — `applyIncoming` inferred deletion-ness from `confirmedDeletions` (so an UNCONFIRMED incoming deletion misclassified as a create and `chezmoi apply` *deleted* the file). Now classifies real incoming deletions via `parseIncomingDeletions(chezmoi status)` (col Y=D), routes them `incoming-delete` regardless of confirmation → the `kind==='delete' && !confirmed` gate is LIVE (refuses `needs-confirmation`, never reaches `applyGuarded`). Both UI seams now gate a deletion behind a Destructive ConfirmDialog before passing `confirmedDeletions` (ReviewApply Apply one/all + Workspace row-Apply). Replaced the vacuous invariant-#4 e2e with a REAL `.chezmoiremove` incoming deletion (verified to fail without the fix). 134 tests green (+20: planner units + property, SyncEngine delete/block/property, `parseIncomingDeletions`, 3 real-binary e2e incl. atomic-TOCTOU + real-deletion confirm/refuse). |
| `1-11` | Conflict resolution — ConflictModel + merge view, auto-merge non-overlapping | AFK | 1-09 | ✅ Done | workflow | 2026-06-16 | 2026-06-16 | feat/main-app | `ConflictModel` is SOLE owner of invariant #1 (never auto-resolve a Conflict): a module-private brand makes `ResolvedConflict` bytes unconstructable except via `resolve(choice)` (Keep mine/Take theirs/Open both). Pure-git cross-env axis — `git fetch`+`git merge` in the source repo auto-merges non-overlapping hunks (no prompt), `UU` overlaps surface as true Conflicts with 3 sides (ours/theirs/marker-union). SyncEngine `routeConflictResolution` accepts ONLY branded user choices; the `@pierre/diffs` `UnresolvedFile` merge view is read-only (`mergeConflictActionsType:'none'`) and never calls the lib's `resolveConflict()`. 148 tests green (+14). |
| `1-12` | Sync-now polish + Auto-sync + TrayPoller + OS notification + AutomationPolicy | AFK | 1-09, 1-03 | ✅ Done | workflow | 2026-06-16 | 2026-06-16 | feat/main-app | `AutomationPolicy` gates the ladder (MVP = Manual + Auto-sync) by **depending on** the invariant-owner types — `mayAutoApply` consumes the planner's `blockedReason`/`requiresConfirmation` + requires an `AppliesHere` witness (never re-checks #2/#3/#4); no `mayAutoCommit` exists (Commit never automatic, ADR 0006). Auto-sync wired into `DenService.commitTracked` (auto-`git push` an ALREADY-Committed change when `mayAutoPush`; `CommitResult.pushed` reflects it). `TrayPoller` (Electron-free, injected scheduler/SHA-reader/notifier seams): cheap `git ls-remote` latest-SHA compare, fetch/notify ONLY on move, adaptive cadence (idle backoff ×2 → ceiling, focus snaps to floor), `onReconnect` for powerMonitor, errors never crash the loop. Real Electron wiring in index.ts: `Tray` (keeps watcher alive window-closed), OS `Notification`, `powerMonitor` resume/unlock, window focus→cadence. Env-local automation level store (userData, ADR 0024) + `automation:get/set-level` IPC + `tray-poller:incoming` push channel; onboarding Auto-sync opt-in now persists; Sync-now/Commit tooltips make transport-not-apply transparent. 174 tests green (+26: AutomationPolicy owner-dependency, TrayPoller cadence/detect/reconnect, settings round-trip, real-binary auto-push vs local, ipc-bridge automation channels). |
| `1-13` | Second-environment onboarding — new-or-returning, subscription pick, claim | AFK | 1-05, 1-11, 1-12 | ✅ Done | workflow | 2026-06-16 | 2026-06-16 | feat/main-app | Returning flow real chezmoi↔main↔IPC↔UI↔tests. NEW SEAM: a **templated `.chezmoiignore`** (`subscription-ignore.ts`, single writer folding `.myenv/`+OS-scope+subscription) self-identifies via `[data].dotden_env_id` and ignores un-subscribed Workspaces' Files → ONE repo materializes different subsets per env (spike-proven, now e2e). Registry-entry guard both ways: **(a) ordering** — `registerWithSubscription`/`setSubscriptions` write the entry (default ALL) BEFORE any apply; **(b) fail-safe** — missing entry/`dotden_env_id` key → `hasKey`+`*` ignore-everything, never errors/apply-all; NEVER silent — `subscriptionState().emptyDenWarning` surfaces why+fix. Un-subscribe keep/remove (`.chezmoiignore`≠delete → `keep` orphans, `remove` deletes only this env's local copy; remembered default in `subscription-settings.ts`). New-or-returning UI: ReturningShell (reuses OBConnectUrl)→OBFoundDen (claim via suggestClaims)→OBPickWorkspaces (default all)→`env:claim`/`register-new`→app Review&Apply (role b). App landing chooser. `den:subscription-state`/`set-subscriptions`/`unsubscribe-workspace`/`unsubscribe-disposition`/`env:register-new`/`env:claim` IPC. 248 tests green (+21: subscription-ignore renderer, settings store, MyenvStore/registry subscription units, ipc routes, 3 real-binary e2e: 2-env subsets + empty-Den fail-safe + keep/remove). Gate green (format/check/test). |
| `1-14` | Workspaces (access boundary, invisible until 2nd) + nested Groups | AFK | 1-07 | ✅ Done | workflow | 2026-06-15 | 2026-06-15 | feat/main-app | Workspace/Group model in synced `.myenv/`: `createWorkspace`/`createGroup`(nested via `parentId`)/`moveFileToGroup`/`setFileWorkspace`; placement gains `groupId`. Group move is organize-ONLY (workspaceId access + targetPath path proven unchanged). Forward-compat read of legacy docs. `den:create-workspace`/`create-group`/`move-to-group`/`set-file-workspace` IPC. Sidebar reveals Workspace sections + nested Groups only once a 2nd Workspace/any Group exists (invisible-until-2nd); inspector Group selector. 106 tests green. Unblocks 1-15. |
| `1-15` | OS Scope + inheritance (narrowable) → per-OS .chezmoiignore | AFK | 1-01, 1-07, 1-14 | ✅ Done | workflow | 2026-06-15 | 2026-06-15 | feat/main-app | OS Scope axis real chezmoi↔main↔IPC↔UI↔tests: pure `os-scope.ts` (intersection-based narrow/effective/scopedOut — broaden is unrepresentable) → synced `.myenv/` Scope on Workspace/Group/File + clamped `setFileScope`/`setGroupScope` + `effectiveScopeOf` inheritance fold → native `.chezmoiignore` (single writer, always re-emits `.myenv/`) → `chezmoi ignored` muted set (tree unions managed+placements so a scoped-out=unmanaged-here File shows muted, not vanished) → `ApplicabilityResolver` gains the `file.scope matches env.os` clause (`out-of-scope`) → `den:set-file-scope`/`set-group-scope` IPC + ScopeEditor inspector. +28 tests (os-scope property, store clamp, resolver OS-axis, real-binary scope→ignore→travels). |
| `1-16` | Offline Commit + queued pushes + retry on reconnect/next Sync | AFK | 1-04, 1-12 | ✅ Done | workflow | 2026-06-16 | 2026-06-16 | feat/main-app | Offline-safe Commit + durable push queue: a Commit always records locally; an offline push (real git "Could not resolve host/Connection refused" signature → `isOfflineError`) is QUEUED in a durable single-flag outbox (dedup, survives restart, no dupes), retried on reconnect (`powerMonitor` + renderer `online`) and next Sync. New `OfflineBanner`. 25 tests (+offline classifier, push-queue, 4 real-binary e2e). |

## PRD 2 — Improvement (history · secrets · settings)

| Issue | Title | Type | Blocked by | Status | Who | Started | Finished | Branch/PR | Outcome |
|-------|-------|------|-----------|--------|-----|---------|----------|-----------|---------|
| `2-01` | History tab — per-File version list + read-only preview | AFK | 1-01, 1-07 | ✅ Done | workflow | 2026-06-16 | 2026-06-16 | feat/main-app | History tab live in the Diff pane (env A managed File): per-File version list derived PURELY from `git log` (no history store, the load-bearing rule) + read-only PatchDiff preview of each version. Pure `file-history.ts` parser (newest-first, newest=Current, short-SHA, total on malformed); `GitTransport.showFile` (`git show <sha> -- <path>`); `DenService.fileHistory`/`fileVersionDiff` (source-path→repo-relative, empty-not-throw on unmanaged); `den:file-history`/`den:file-version-diff` IPC (read-only, _trace asserted) + contract + preload. Renderer `CommitRow.tsx` (git-commit lead · message over amber shortSha·timestamp · green Current pill · disclosure chevron · loud ember-rail Selected) + `FileHistory.tsx` master-detail (scrollable list ⟂ raised card preview, independent scroll, self-contained draggable resize handle — no new dep, "Kept in history — nothing is deleted" reassurance, read-only badge). Wired the previously-stubbed History tab in `Workspace.tsx`. Restore-forward = 2-02. 306 tests green (+9: parser units, showFile + per-File e2e history/preview, ipc routes). |
| `2-02` | Restore-forward + non-destructive confirm | AFK | 1-04, 2-01 | ✅ Done | workflow | 2026-06-16 | 2026-06-16 | feat/main-app | Single Restore-forward action on the previewed version (never per-row) + Default-tone (non-danger) confirm. `DenService.restoreFileVersion` = `git show <sha>:<path>` (new `GitTransport.readFileAtCommit`) → write source forward → `chezmoi apply` → `GitTransport.commitIfChanged` (now returns whether it committed) = a NEW commit, history never rewritten; restoring Current onto itself is a clean no-op (`committed:false`). Unmanaged File throws (never fail silently). `den:restore-version` IPC (mutates → `_trace` forwarded) + contract (`RestoreResult`) + preload. UI: `FileHistory.tsx` filled-ember `Restore this version` (rotate-ccw) in the preview panel → `ConfirmDialog tone="default"` (added optional ember badge slot) copy "Saved as a new commit; your current version stays in history" → re-reads the list so the restore appears on top with the prior Current still reachable below. **Load-bearing tdd at the ChezmoiAdapter/GitTransport seam** (real chezmoi+temp git): a NEW commit produced (HEAD^ == prior HEAD), every prior version stays reachable, source==restored bytes, destination materialized, no-op + unmanaged-throw cases. 374 tests green (+1 e2e, +ipc route). |
| `2-03` | SecretScanner — commit-time scan + warn step | AFK | 1-01, 1-04 | ✅ Done | workflow | | 2026-06-16 | feat/main-app | Pure `secret-scanner.ts` (regex + Shannon entropy, NO shell/IO) emits `{file,kind,line,maskedValue}` for AWS/GitHub/GitLab/Slack/Google/Stripe/OpenAI/JWT/PEM-key-block + entropy-gated generic shapes, with placeholder/low-entropy false-positive guards; `maskSecret` head/tail-reveal + constant-width elision never exposes the full value (TDD'd, 34 unit tests). Wired at the commit seam: `DenService.scanCommit` reads the about-to-be-committed bytes → `den:scan-commit` IPC → renderer runs it BEFORE `commit`; on findings the amber `SecretWarning` warn step opens (Cancel / Commit anyway) — WARNS, never blocks (ADR 0001). 297 tests green. Convert-vs-Commit choice + allowlist = 2-04, PM picker = 2-05. |
| `2-04` | Commit-anyway + synced per-File 'don't warn' allowlist | AFK | 2-03 | ✅ Done | workflow | | 2026-06-16 | feat/main-app | Step 1 now presents the deliberate two-option choice (Convert to a Secret reference = recommended default / Commit the secret anyway) as a radio group in `SecretWarning.tsx`; under Commit-anyway a per-File "Don't warn me about this File again" checkbox. Pure `secret-allowlist.ts` (TDD, 15 units): `findingFingerprint` scoped per **File+kind+maskedValue** (NOT line) so a new/different secret in an allowlisted File STILL warns — never silently re-enables a leak; `addAllowlistEntry` (immutable, deduped), `partitionFindings`. Synced persistence at the write seam: `MyenvStore.readSecretAllowlist`/`addSecretAllowlistEntry` → `.myenv/secret-allowlist.json` (masked preview only, never raw; ADR 0024); `DenService.scanCommit` filters allowlisted findings, `DenService.allowlistSecret` records (staged into the same Commit that stages `.myenv/`). `den:allowlist-secret` IPC + contract + preload; `secretAllowlistedCount` added to the tracer key allowlist (count only, ADR 0007). e2e: allowlisted File stops warning + NEW secret still warns; allowlist syncs env A→env B (not re-warned). Gate green, 326 tests (+19). PM picker/convert = 2-05. |
| `2-05` | PM picker + convert to a Secret reference | AFK | 1-01, 2-03, 2-04 | ✅ Done | workflow | | 2026-06-16 | feat/main-app | Step-2 PM picker + the chezmoi `.tmpl` convert seam, real chezmoi/git↔main↔IPC↔UI↔tests. Pure `secret-reference.ts` (TDD, 18 units): `renderSecretReferenceTemplate` emits the exact per-manager shape — 1Password `{{ onepasswordRead "op://vault/item/field" }}` (+ account 2nd arg when non-default), Bitwarden `{{ (bitwardenFields "item" "<name>").<field>.value }}`, pass `{{ pass "<entry>" }}` — with quote-escaping + empty-ref reject; `sourceTemplateName` encodes `dot_…`+`.tmpl`; `isSecretReferenceResolutionFailure` classifies a locked/missing PM apply error. `pm-detect.ts` (injectable probe, 4 units) detects op/bw/pass on PATH (env-local, never synced); `pm-preference.ts` (7 units) persists "Remember my choice" under userData. **Integration seam (the Testing Decision)**: `ChezmoiAdapter.convertToSecretReference` writes the `.tmpl` reference + removes the raw source entry — proven by 2 real-chezmoi e2e (raw secret NEVER in source bytes; Apply against a FAKED `op` shim resolves the value). `DenService.convertSecret` orchestrates (write→remember→Commit only the reference) + maps a PM-resolution Apply failure (which breaks `chezmoi status` template render) to a clean provider-agnostic `secret-reference-unresolved` per-File result; 3 DenService e2e (committed source grep-clean of the raw secret, missing-CLI→clean error, faked-op resolves). IPC `den:detect-password-managers`/`den:pm-preference`/`den:convert-secret` + contract + preload. UI (secret-and-errors Step 2): new `SecretPicker.tsx` (ember lock badge, SelectRow rows w/ green "CLI detected"/neutral "Not found"+install-hint Pills, reference input, Remember toggle, Back/Convert) over pure `pm-picker.ts` (9 units: default-select 1Password when op detected / remembered-choice-wins / canConvert); wired into `SecretWarning.onConvert`→detect+open picker→`convertSecret`. 373 tests green (+47). |
| `2-06` | Secret reference resolves on a second environment | AFK | 2-05, 1-13 | ⬜ Not started | | | | | |
| `2-07` | Onboarding Discover Warn reconciliation | AFK | 2-03, 1-06 | ✅ Done | workflow | 2026-06-16 | 2026-06-16 | feat/main-app | Discover rows pre-scan via `den.scanCommit`; secret-bearing Files show amber selectable Warn (never excluded), resolved at Commit |
| `2-08` | SettingsShell + Sync tab | AFK | 1-06, 1-07, 1-12 | ✅ Done | workflow | | 2026-06-16 | feat/main-app | SettingsShell (Titlebar + 248px rail + content-swap) driven by a one-list extensible tab registry; live Sync tab (poller on/off + cadence + start-on-login over a new env-local `sync-settings` store/IPC seam, ADR 0024) + "Sync now never auto-applies" copy; 6 inert placeholders. 261 tests green. |
| `2-09` | Commit tab — message-template editor | AFK | 2-08 | ✅ Done | workflow | 2026-06-16 | 2026-06-16 | feat/main-app | Settings → Commit tab live: mono editor + insertable `$var` chips (all 12, Kbd-style hints) + live preview + Reset to default. New SHARED pure `commit-template.ts` (`$var` scheme — single source for the preview AND the real message; darwin→macos rename, app-clock date/time) backs both, so NO shell is reachable from the renderer. os/arch/hostname sourced via new `ChezmoiAdapter.templateData()` (`chezmoi execute-template .chezmoi.os/arch/hostname`). Synced default in `.myenv/commit-template.json` (MyenvStore read/write + ensureIgnored); `DenService.commitTemplate`/`setCommitTemplate` Commit `.myenv/` locally → travels. `den:get/set-commit-template` IPC + contract + preload; tab flipped placeholder→live in the registry. 389 tests green (+15: 9 pure-renderer, 3 store round-trip, adapter e2e, bridge routes, den-service e2e travels-to-2nd-env). |
| `2-10` | Appearance + default Apply/notification preferences controls | AFK | 2-08 | ⬜ Not started | | | | | |
| `2-11` | Account tab — GitHub + detected PM CLI + reconnect | AFK | 1-03, 2-05, 2-08 | ⬜ Not started | | | | | |
| `2-12` | Automation tab + Auto-apply path | AFK | 2-08, 1-09, 1-10, 1-11, 1-12, 1-14, 1-15 | ⬜ Not started | | | | | |
| `2-13` | YOLO mode — strongly-warned full hands-off | AFK | 2-12, 1-10, 1-11, 1-14, 1-15 | ⬜ Not started | | | | | |
| `2-14` | Privacy tab — opt-in toggles + copy (control surface only) | AFK | 2-08 | ⬜ Not started | | | | | |
| `2-15` | Environments tab + claim / reassign / retire lifecycle | AFK | 2-08, 1-05, 1-13 | ⬜ Not started | | | | | |
| `2-16` | About tab — version, update check, chezmoi credit | AFK | 2-08 | ⬜ Not started | | | | | |
| `2-17` | Synced-vs-local settings — defaults + local override | AFK | 2-09, 2-10, 2-14, 1-05 | ⬜ Not started | | | | | |

## PRD 3 — Polish (distribution · telemetry · feedback · state surfaces)

| Issue | Title | Type | Blocked by | Status | Who | Started | Finished | Branch/PR | Outcome |
|-------|-------|------|-----------|--------|-----|---------|----------|-----------|---------|
| `3-01` | Commit instrumented end-to-end: one allowlist-typed Wide event into the always-on ring buffer | AFK | 1-04 | ⬜ Not started | | | | | |
| `3-02` | Extend instrumentation to the remaining operations + tail sampling governs buffer size | AFK | 3-01 | ⬜ Not started | | | | | |
| `3-03` | TraceContextCodec — trace_id across the renderer-main IPC boundary (relay half deferred) | AFK | 1-04, 3-01 | ⬜ Not started | | | | | |
| `3-04` | Banner status strip wired to live Sync state (six tones + CTAs) | AFK | 1-09, 1-12 | ⬜ Not started | | | | | |
| `3-05` | Toast transient success/info confirmations | AFK | 3-04 | ⬜ Not started | | | | | |
| `3-06` | Native macOS tray menu wired to live state + actions | AFK | 1-09, 1-12, 3-04, 2-12, 2-13 | ⬜ Not started | | | | | |
| `3-07` | OS notifications for cross-environment activity (notify-only, native macOS chrome) | AFK | 1-12, 3-06, 2-12 | ⬜ Not started | | | | | |
| `3-08` | Offline + apply-error screens with Retry (per-file atomicity, empty incoming review) | AFK | 1-09, 1-16, 3-04 | ⬜ Not started | | | | | |
| `3-09` | First-launch consent screen + egress-gate wiring (two independent opt-ins, off by default) | AFK | 3-01, 2-14 | ⬜ Not started | | | | | |
| `3-10` | TelemetrySink consent gate proven to suppress egress (the load-bearing privacy test) | AFK | 3-02, 3-09 | ⬜ Not started | | | | | |
| `3-11` | Sentry egress mapping + PII scrub in beforeSend (faked transport, gate-closed) | AFK | 3-10 | ⬜ Not started | | | | | |
| `3-12` | Sentry provisioning + end-to-end egress verification (DSN) | HITL | 3-11 | ⬜ Not started | | | | | |
| `3-13` | Umami payload mapping (cookieless counts + engagement events, faked transport) | AFK | 3-10 | ⬜ Not started | | | | | |
| `3-14` | Umami provisioning + end-to-end egress verification (self-hosted instance) | HITL | 3-13 | ⬜ Not started | | | | | |
| `3-15` | In-app feedback form: compose, preview, attach scrubbed log (from known path) | AFK | 3-02 | ⬜ Not started | | | | | |
| `3-16` | Screenshot attachment + structural attachment contract (magic-byte, EXIF-strip, size-cap) | AFK | 3-15 | ⬜ Not started | | | | | |
| `3-17` | Feedback offline queue + relay client (trace_id propagated on the relay boundary) | AFK | 3-03, 3-16 | ⬜ Not started | | | | | |
| `3-18` | Relay deployment: moderated GitHub issues, rate-limit, attestation, server-side scrub | HITL | 3-17 | ⬜ Not started | | | | | |
| `3-19` | electron-builder packaging: dmg / NSIS / AppImage with pinned chezmoi (unsigned) | AFK | — | ✅ Done | workflow | 2026-06-16 | 2026-06-16 | feat/main-app | Pinned, checksum-verified bundle real end-to-end: `resources/bin/tools.lock.json` (chezmoi 2.70.5 + dugite-native git 2.53.0, per-target sha256) + `scripts/fetch-binaries.mjs` (download→verify→extract into the tools.ts layout) wired as electron-builder `beforePack`; `.yml` already produces dmg/NSIS/AppImage unsigned; electron-updater already a runtime dep. Proven by running the host fetch (chezmoi/git report pinned versions) + an opt-in `DOTDEN_FETCH_SMOKE` boot smoke + a hermetic lock↔resolver contract test. 3-20 unblocked. |
| `3-20` | Auto-update from GitHub Releases: background download, apply-on-restart with defer, version surface | AFK | 3-19, 2-16 | ⬜ Not started | | | | | |
| `3-21` | Wire the GitHub Releases publish feed + first release (fill REPLACE_ME) | HITL | 3-20 | ⬜ Not started | | | | | |
| `3-22` | CI pipeline: frozen-lockfile install with ADR 0015 guards + turbo affected check/build | AFK | — | ✅ Done | workflow | 2026-06-16 | 2026-06-16 | feat/main-app | `.github/workflows/ci.yml`: corepack enable → `pnpm install --frozen-lockfile` (still enforces ADR 0015 guards: allowBuilds default-deny, strict minimumReleaseAge, hoisted) → `turbo run check:types check:lint build test --affected`. Added a `test` turbo task + root `test` script so the gate's `pnpm test` resolves and CI tests affected packages. `fetch-depth:0` + `TURBO_SCM_BASE=github.event.before` on push (turbo auto-detects GITHUB_BASE_REF on PRs). Gate green. |
| `3-23` | OSS LICENSE choice (MIT vs Apache-2.0) | HITL | — | ⬜ Not started | | | | | |

## Log (append-only, newest first)

<!-- Add one line per state change: `YYYY-MM-DD · code · status · agent · note` -->
2026-06-16 · CODE · ✅ Done · workflow · 2-09 Commit tab — message-template editor, real chezmoi/git↔main↔IPC↔UI↔tests (settings.md "Commit"). Flipped the Commit tab placeholder→`live` in the SettingsShell tab registry (the clean extensible seam 2-08 built — one entry + a content file, no shell change). **New SHARED pure `shared/commit-template.ts`** (Electron-free, TDD'd 9 units) is the SINGLE source of truth for the `$var` scheme (mapping to chezmoi `git.commitMessageTemplate`): `COMMIT_TEMPLATE_VARIABLES` (the closed 12 — `$os $arch $hostname $environment $year $month $day $hour $minute $date $time $filecount`), `DEFAULT_COMMIT_MESSAGE_TEMPLATE = '[$os-sync-$year-$month-$day]'`, and `renderCommitTemplate` which interpolates ONLY that closed set (unknown `$tokens` left verbatim → never crash a Commit; longest-name-first + `\b` so `$filecount`/`$yearly` never mis-split). It backs BOTH the tab's live preview AND (via PRD2#17) the real message, so the preview can never drift. **No-shell discipline (the load-bearing privacy rule, scope-v1 / ADR 0007)**: os/arch/hostname come from chezmoi template data — new **`ChezmoiAdapter.templateData()`** = `chezmoi execute-template '{{ .chezmoi.os }}\x1f…'` (US-separated so a spaced hostname can't mis-split), with the one presentation rename (`darwin`→`macos`) owned by the pure renderer's `normalizeOs` — and date/time come from the **app runtime clock** (`new Date()` captured once via a lazy `useState` initializer, NOT a ref, to satisfy react-hooks/refs), NEVER an OS `date` command; nothing in the renderer shells out. **Synced default** (user-authored presentation → syncs, ADR 0024): `.myenv/commit-template.json` via new `MyenvStore.readCommitTemplate`/`writeCommitTemplate` (empty/malformed → built-in default, never a blank message; `writeCommitTemplate` calls `ensureIgnored` so the first `.myenv/` write on a never-seeded Den still creates `.chezmoiignore` and the metadata Commit can stage it). `DenService.commitTemplate` (read template + chezmoi data + env label) / `setCommitTemplate` (write + `commitMetadata` LOCAL Commit, ADR 0006 → travels on next Sync; empty→default). **IPC** `den:get-commit-template` (read Operation) / `den:set-commit-template` (MUTATES → trace id forwarded) + contract (`CommitTemplateState`) + preload. **UI** `CommitTab.tsx`: mono `<textarea>` editor, insertable variable chips (click → insert `$token` at caret; `title` hint of each var's source) with the os/arch/hostname-from-chezmoi vs date/time-from-clock note, an ember live-preview card rendered by the shared pure fn (`fileCount:1` illustration), filled-ember Save (disabled unless dirty) + outline Reset-to-default (disabled when already default) + "Unsaved changes" marker; optimistic-then-authoritative save mirroring SyncTab, inline error never silent. 389 tests green (+15: 9 pure-renderer incl. closed-set/longest-name/darwin-rename/zero-pad, 3 MyenvStore round-trip incl. default-fallback, 1 `templateData` real-chezmoi adapter e2e, 1 ipc-bridge commit-template route block asserting trace-forward + `_trace`-missing reject, 1 DenService real-chezmoi e2e proving defaults→persist→**travels to a 2nd cloned env** with that env's own local label + reset round-trip). Gate green (format · check 5/5 · test). No new ADR (faithful realization of ADR 0003 maps-to-chezmoi `git.commitMessageTemplate` + scope-v1 "Customizable commit-message template" closed-var list + cross-OS-safe sourcing + ADR 0024 template-syncs-as-default). docs/design-system/status.md settings row note extended (Commit tab now built). Full-synced-vs-local override stays PRD2#17.
2026-06-16 · CODE · ✅ Done · workflow · 2-02 Restore-forward + non-destructive confirm, real chezmoi/git↔main↔IPC↔UI↔tests. **Restore-FORWARD, never rewrite history** (ADR 0003 faithful wrapper — every Commit is a git commit, so a restore is just another commit whose source content equals the old version): `DenService.restoreFileVersion(targetPath, sha, trace)` resolves the File's `chezmoi source-path` → repo-relative, reads the EXACT source bytes that version had via new **`GitTransport.readFileAtCommit(sha, path)`** = `git show <sha>:<path>` (the blob, not a diff — distinct from `showFile`'s `<sha> -- <path>` patch), writes them FORWARD over the current source file (the previewed version's content becomes the new source state), `chezmoi apply <file>` materializes it onto the destination/home copy, then **`GitTransport.commitIfChanged`** records a NEW commit on top of history. commitIfChanged now RETURNS whether it committed (backward-compatible — `commitMetadata` ignores it), so restoring the **Current** version onto itself stages no change → clean no-op (`RestoreResult.committed:false`), never an invented empty commit, never a failure. An unmanaged/unresolvable File THROWS (never fail silently — Restore is only offered on a managed File's real version). Traced as a `commit` Operation (it produces a commit — no new tracer kind, allowlist intact). **IPC**: `den:restore-version` (MUTATES the Den → its `_trace` id IS forwarded, unlike the two read-only History calls) + contract (`RestoreResult`) + preload. **UI** (file-history.md Screen 2): `FileHistory.tsx` preview panel now carries the SINGLE **filled ember Primary `Restore this version`** button (`rotate-ccw` glyph) — only here, NEVER per-row, so the version it restores is unambiguous (a row button + panel button = two restores for one version); on click it opens the `ConfirmDialog` in **`tone="default"`** (extended with an optional ember `badge` slot for the `rotate-ccw` badge — NEVER the destructive red reserved for Delete, because restore-forward loses nothing) with the exact copy **"Saved as a new commit; your current version stays in history"**; confirming → `den.restoreVersion` → re-reads the version list (shared `applyVersions` past an `await`, so set-state-in-effect stays clean) which surfaces the restore on top with the prior **Current** version still reachable below — the visible proof nothing was destroyed. **Load-bearing tdd at the ChezmoiAdapter/GitTransport integration seam** (real chezmoi + temp git, the issue's Testing Decision): commit 3 versions, restore the oldest forward → a NEW commit is produced (`HEAD^` == the prior HEAD, history APPENDED not rewritten), the list grows by one with EVERY prior version still reachable (the prior Current included), the new source bytes EQUAL the restored version's `git show <sha>:dot_zshrc`, the destination File is materialized back (PAGER gone), the source tree is clean, a `commit` wide event landed; plus the no-op case (restore Current onto itself → `committed:false`, HEAD unchanged) and the unmanaged-File throw. 374 tests green (+1 e2e; +ipc-bridge restore route asserting the trace id IS forwarded). Gate green (format · check 5/5 · test). docs/design-system/status.md file-history row → ✅ built (Screen 2 described). No new ADR (faithful realization of ADR 0003 git-log-is-history + scope-v1 "History" restore-this-version-captures-forward + the Default-tone non-destructive treatment already specced in file-history.md; commitIfChanged returning a bool + the ConfirmDialog badge slot are small additive reuse, not hard/surprising decisions).
2026-06-16 · CODE · ✅ Done · workflow · 2-05 PM picker + convert to a Secret reference, real chezmoi/git↔main↔IPC↔UI↔tests — **the one narrow guided slice of chezmoi templating (scope-v1 Secrets)**. **Pure `secret-reference.ts`** (Electron-free, TDD'd 18 units): `renderSecretReferenceTemplate` emits the EXACT per-manager template call — 1Password `{{ onepasswordRead "op://vault/item/field" }}` (+ `"account"` 2nd arg when non-default), Bitwarden `{{ (bitwardenFields "item" "<name>").<field>.value }}`, pass `{{ pass "<entry>" }}` — Go-string-escaped + empty-ref reject; the rendered string is a *reference/template call, NEVER the raw value* (the security invariant, pinned by unit). `sourceTemplateName` encodes `dot_…` + idempotent `.tmpl`; `isSecretReferenceResolutionFailure` classifies a locked/signed-out/missing-item PM apply error (provider-spanning, so one detector covers op/bw/pass). **`pm-detect.ts`** (injectable PATH probe, 4 units) detects op/bw/pass via no-shell `which`/`where` — **env-local, never synced** (ADR 0024), every manager returned w/ `available` + its `installHint` so an absent CLI shows the disabled option's why. **`pm-preference.ts`** (7 units) persists "Remember my choice" under Electron `userData` (env-local). **Integration seam (the Testing Decision)**: `ChezmoiAdapter.convertToSecretReference` writes the `.tmpl` reference into source state + removes the raw source entry (the value is not even passed in) — proven by 2 real-chezmoi e2e: the written source bytes are grep-clean of the raw secret, and Apply against a FAKED `op` shim resolves to the vault value. `DenService.convertSecret` orchestrates write→remember→Commit-only-the-reference + maps a PM-resolution Apply failure to a clean provider-agnostic `secret-reference-unresolved` per-File result (3 e2e: committed source grep-clean, missing-CLI→clean error, faked-op resolves criterion 8). **IPC** `den:detect-password-managers`/`den:pm-preference`/`den:convert-secret` (+ contract + preload, `_trace` forwarded; detect/pref are read-but-traced). **UI** (secret-and-errors Step 2): new `SecretPicker.tsx` (ember `lock` badge, SelectRow rows w/ green "CLI detected" / neutral "Not found"+install-hint Pills, reference input, Remember toggle, Back/Convert footer) over pure `pm-picker.ts` (9 units: default-select 1Password when op detected / remembered-choice-wins-if-still-installed / canConvert); wired into `SecretWarning.onConvert` → detect + open picker → `convertSecret`. 373 tests green (+47). Gate green (format · check 5/5 · test). No new ADR (faithful realization of ADR 0003 every-concept-maps-to-chezmoi via the named `onepasswordRead`/`bitwardenFields`/`pass` functions + scope-v1 "Secrets" one-narrow-templating-slice + ADR 0024 PM-choice/CLI-presence env-local). No docs/design-system/status.md change (the SecretPicker Step 2 screen was already specced in secret-and-errors.md; this realizes it in code). Unblocks 2-06 · 2-11.
2026-06-16 · CODE · ✅ Done · workflow · 2-07 Onboarding Discover Warn reconciliation, real chezmoi/git↔main↔IPC↔UI↔tests — **reconciles the SecretScanner (2-03) into the onboarding Discover step as warn-not-block**, no parallel detector and no new owner. **Reuse over fork**: after the discovery scan resolves, `OBDiscover` pre-scans the discovered Files via the EXISTING `window.dotden.den.scanCommit(paths)` — the exact commit-time detector + synced-allowlist filter (issues 2-03/2-04) — so onboarding and Commit agree on what looks secret-bearing (`scanCommit` reads destination bytes by target path, which the discovered home-dir Files already are, so it works pre-track/pre-commit). **Pure derivation seam** `src/renderer/components/onboarding/secret-warn.ts` (Electron-/DOM-free, TDD'd 4 units): `warnedPathsFromFindings(findings)` folds the flat `SecretFinding[]` into the **set of File paths to flag** — warn is **per-File, not per-finding** (a File with many secrets = one warned row); a clean/empty scan flags nothing. Kept pure so the mapping is unit-tested independent of the component (mirrors the scanner's own posture). **`ListRow` Warn state** (onboarding.md `ListRow` Warn = Batch-E soft-warn): new `warn` prop renders the amber state — a **real, selectable** `Checkbox` (NOT auto-deselected, NOT Blocked/excluded), the icon swapped to an amber `AlertTriangle` (warn tone, never destructive-red — catching a secret is non-destructive, the remedy is safe), and the size meta replaced by an amber **"Secret · review at commit"** label + a faint amber row edge when unselected. The File stays Trackable; **a Tracked warned File routes through the commit-time warn step (`SecretWarning`, 2-03) when later Committed** — flagged here, resolved there (Convert to a Secret reference / Commit anyway). **`OBDiscover`** holds a `warnedPaths` set, populates it from the mount scan AND re-scans each drag-in/browse-added custom File, passes `warn={warnedPaths.has(path)}` per row; the secret pre-scan is **best-effort + non-blocking** (a failed scan leaves rows neutral and is swallowed — the commit-time warn step is the real guard, ADR 0001, never fail the Discover step over an advisory scan). Discover subtitle reworded to "Secrets are flagged so you store them safely, never synced raw." (onboarding.md). 330 tests green (+4: per-File collapse, no-finding stays neutral, clean/empty flags nothing). Gate green (format · check 5/5 · test). No new ADR (faithful application of ADR 0001 warn-not-block + roadmap "Soft-block detected secrets" reconciliation + onboarding.md `ListRow` Warn spec; the reuse of `scanCommit` over a new onboarding-scan channel is a small reuse choice, not a hard/surprising decision). No docs/design-system/status.md change (ListRow Warn was already specced; this realizes it in code).
2026-06-16 · CODE · ✅ Done · workflow · 2-01 History tab — per-File version list + read-only preview, real git↔main↔IPC↔UI↔tests. **Load-bearing rule honored**: the version list is derived PURELY from `git log` scoped to the File — there is NO separate history store (acceptance criterion + ADR 0003 faithful wrapper: every Commit is already a git commit, so a File's history IS its git log). **Pure parser** `src/main/foundation/file-history.ts` (Electron-free, tdd'd): `parseFileHistory(rawGitLog)` splits the exact `%H␟%an␟%ae␟%aI␟%s` format `GitTransport.log` emits into the ordered `FileVersion[]` (full SHA + 7-char `shortSha` + message + author + ISO `committedAt`), NEWEST FIRST (git's default order), flagging the newest as **Current** (the version matching the current Den state — exactly one, or none when empty); empty/whitespace → empty list (a File tracked-but-never-committed, or a fresh repo — honest empty state, never throw); a malformed line degrades each missing field to "" so one odd commit never blanks the tab. **`GitTransport.showFile(sha, path)`** = `git show <sha> -- <path>` (the read-only patch one version introduced for one File; empty when the version didn't touch it, no checkout/mutate). **`DenService.fileHistory`/`fileVersionDiff`**: resolve the File's `chezmoi source-path` → repo-relative (so `git log`/`git show` see the right pathspec) → log/show; an unmanaged/unresolvable path returns []/'' rather than throwing the tab open red (never fail silently). Both READ-ONLY (no wide event; IpcBridge still asserts `_trace`). **IPC**: `den:file-history`/`den:file-version-diff` + contract (`FileVersion`) + preload. **UI** (file-history.md, Screen 1): new `CommitRow.tsx` (the `CommitRow` SET — `git-commit` lead ember-tinted when Selected, `message` over **amber** `shortSha · timestamp`, green **Current** pill, trailing disclosure chevron; loud Selected = secondary bg + 2px ember left rail + ember dot; NO per-row restore — restore is the single panel action in 2-02) + `FileHistory.tsx` (the `AppPane/History` master-detail body: a scrollable `CommitRow` list ⟂ a raised `card` preview panel that swaps to the selected version via the SAME read-only `@pierre/diffs` `PatchDiff` role — `read-only` badge, no resolve/edit, "Kept in history — nothing is deleted" reassurance line; the two regions scroll INDEPENDENTLY, split by a self-contained draggable resize handle with a grip pill — NO `react-resizable-panels` dep added, honoring the package-averse preference; newest auto-selected so the preview is never blank; per-File only — no Den-wide timeline). Wired the previously-stubbed History tab in `Workspace.tsx` (selectable for a managed File on env A; snaps back to Changes when selection clears; `FileHistory` keyed by selected File so a File switch remounts it, which is also how the reset avoids set-state-in-effect). **The Restore this version action + non-destructive confirm Dialog (Screen 2) is issue 2-02** — this slice is read-only preview. 306 tests green (+9: parser units — newest-first/Current-only/short-SHA/empty/malformed-total; `GitTransport.showFile` per-commit patch + empty-on-untouched; DenService e2e — 3-version per-File history newest-first+Current, per-File scope isolation, read-only preview of newest+first version, unmanaged→empty; ipc-bridge file-history/file-version-diff routes). Gate green (format · check 5/5 · test). No new ADR (faithful implementation of ADR 0003 git-log-is-history + scope-v1 "History" per-File + the established read-only-PatchDiff role; the in-component resize handle vs a new dep is a small build choice, not a hard/surprising architectural decision). Unblocks 2-02.
2026-06-16 · CODE · ✅ Done · workflow · 2-04 Commit-anyway + synced per-File 'don't warn' allowlist, real chezmoi/git↔main↔IPC↔UI↔tests. **Step 1 is now the deliberate two-option choice** (secret-and-errors spec): `SecretWarning.tsx` presents a mutually-exclusive radio group — **Convert to a Secret reference** (recommended, selected by default → step-2 PM picker, issue 2-05, wired via optional `onConvert`) vs **Commit the secret anyway**; under Commit-anyway a per-File **"Don't warn me about this File again"** checkbox appears (the choice/checkbox reset is done by re-mounting the modal per warn session with a `key` — no reset effect, keeping state changes out of effects per react-patterns/set-state-in-effect). **Pure `secret-allowlist.ts`** (Electron-free, TDD'd, 15 units): `findingFingerprint` = a short SHA-256 over **File + kind + maskedValue** (NUL-joined) — deliberately **NOT line** (a moved secret stays allowlisted) — so the allowlist is scoped **per File + the specific match**, NOT per File; this is the security crux: a NEW/different secret in an already-allowlisted File still warns (a real leak is never silently re-enabled). `isAllowlisted`/`addAllowlistEntry` (immutable, deduped on fingerprint)/`partitionFindings` (toWarn vs allowlisted). **Synced persistence at the write seam** (the acceptance criterion): `MyenvStore.readSecretAllowlist`/`addSecretAllowlistEntry` → `.myenv/secret-allowlist.json` (forward-compat empty default; stores ONLY the masked preview + fingerprint, never the raw secret, so nothing leaks into sync; idempotent write = no git churn); it's `.myenv/` so it SYNCS through the Remote (ADR 0024). **DenService**: `scanCommit` now reads the synced allowlist and `partitionFindings` to return only the findings to warn about (allowlisted ones filtered out) — scoped per File+match; new `allowlistSecret(finding, trace)` records a dismissal (staged into the SAME Commit that already stages `.myenv/`, so it travels with the next Sync); both emit COUNT-only attributes. New allowlisted tracer key `secretAllowlistedCount` (a number only — value/path/File still unrepresentable, ADR 0007 privacy owner). **IPC**: `den:allowlist-secret` (mutates synced metadata, `_trace` forwarded) + preload + contract (`SecretAllowlist`). **Renderer**: Workspace's `commitAnyway(findings, paths, dontWarnAgain)` allowlists each shown finding first (when ticked) then records the Commit in one `run` span (extracted `recordCommit` so the allowlist+commit share a single span); the warn step never blocks the Commit (warn-not-block, ADR 0001). e2e (real chezmoi/git): an allowlisted File stops warning on later Commits BUT a new GitHub token added to it still warns (`['GitHub Token']`); the allowlist SYNCS env A→env B (env B reads the decision from `.myenv/` + does NOT re-warn the same value). 326 tests green (+19: allowlist model units, MyenvStore persistence seam, 2 cross-env e2e). Gate green (format · check 5/5 · test). No new ADR (faithful implementation of ADR 0024 "Secret-scan 'sync anyway' allowlist decisions" already listed as synced `.myenv/` data + scope-v1 Secrets + ADR 0001 warn-not-block). PM picker/convert (step 2) = 2-05.
2026-06-16 · CODE · ✅ Done · workflow · 2-03 SecretScanner — commit-time scan + warn step, real chezmoi/git↔main↔IPC↔UI↔tests. **Pure detector** `src/main/foundation/secret-scanner.ts` (Electron-free, NO shell/IO/network — the only IO is the caller reading File bytes): a regex+Shannon-entropy table emits findings of the issue's exact shape `{ file, kind, line, maskedValue }`. **SHAPE detectors** (known prefix/format, not entropy-gated) catch AWS Access Key ID (`AKIA…`/`ASIA…`) + Secret Access Key, GitHub (`ghp_`/`gho_`/…/`github_pat_`), GitLab (`glpat-`), Slack (`xox[baprs]-`), Google (`AIza…`), Stripe (`sk_live`/`sk_test`/`rk_`), OpenAI (`sk-…`), JWT (`eyJ….….…`), and PEM/OpenSSH/PGP **private-key blocks**; a **GENERIC catch-all** flags a credential-named assignment (`api_key`/`secret`/`token`/`password`/…) to a longish HIGH-entropy value, entropy-gated so `token = mytoken` / `password = changeme` don't trip it. **False-positive posture** (`isPlaceholder`): obvious placeholders (`xxxx`, `<your-token>`, `${VAR}`, `changeme`, `example`, all-same-char fills) are skipped so the warn step stays trustworthy — notably AWS's own `…EXAMPLE` example keys are correctly NOT flagged. **`maskSecret` is the security invariant**: long values reveal only a 4-char head+tail (`AKIA••••••••••••N7QX`), short values reveal bullets ONLY, and the elision is a CONSTANT-width bullet run so neither the middle nor the hidden length leaks — the full value never reaches the UI (TDD'd: the masked preview never contains the raw value). **WARN, never block (ADR 0001)**: `DenService.scanCommit(paths, trace)` reads each about-to-be-Committed File's on-disk bytes (the bytes chezmoi re-add would import — catching the secret "at the door"), runs the pure scanner, and returns findings as DATA — it has no power to prevent a Commit; an unreadable File is skipped (advisory, never fails the Commit). New allowlisted tracer key `secretFindingCount` (a COUNT only — the value/path/kind are never attributes, so the wide event records "flagged N" without carrying a secret). **Seam**: `den:scan-commit` IPC (a read-but-Operation channel, `_trace` forwarded) + preload + contract (`scanCommit`). **UI** (secret-and-errors screen spec, Step 1): new `SecretWarning.tsx` (`@base-ui` alert-dialog over the scrim-dimmed home, like ConfirmDialog) — **amber** `alert-triangle` badge (NOT destructive-red — a secret is a caution, the remedy is safe), one detected card per finding (File mono + amber `SECRET` pill + kind·line + the masked preview), footer **Cancel** / **Commit anyway**. Workspace's commit flow (toolbar Commit + row-verb Commit) now SCANS first via `commitWithScan`; findings open the warn step which proceeds to the actual Commit on "Commit anyway", else commits straight through. The deliberate two-option Convert-vs-Commit choice + per-File "don't warn again" allowlist (full Step 1) and the `SecretPicker` Step 2 PM picker are issues 2-04/2-05. 297 tests green (+34 scanner units at the seam: every shape, the false-positive guards, the mask never-exposes invariant, the finding shape/line numbers; +2 real-binary e2e: scan flags a real AWS key on the right File/line masked then the Commit STILL proceeds + records `secretFindingCount:1`, and a clean set returns no findings; +ipc-bridge scan-commit channel). Gate green (format · check 5/5 · test). No new ADR (faithful implementation of ADR 0001 pure-git + scope-v1 "Secrets" warn-not-block + ADR 0024 allowlist-is-synced, all already specified). Unblocks 2-04 · 2-05 · 2-07.
2026-06-16 · CODE · ✅ Done · workflow · 2-08 SettingsShell + Sync tab — real chezmoi/git↔main↔IPC↔UI↔tests. **Extensible tab registry** (`renderer/components/settings/tabs.tsx`): the ONE list every Settings tab registers in — `SettingsShell` renders its 248px nav rail AND its content area straight from it, so a later tab slice (2-09/11/12/14/15/16) appends ONE `SettingsTab` entry (id · label · lucide icon · `status` · `Content`) and lights up with zero shell churn. All seven v1 tabs declared up front to fix the rail SHAPE, but only `live` ones are selectable; the other six render **inert/disabled placeholders** (greyed + "Soon", non-clickable, honest "coming in a later release" empty state — never advertise unbuilt UI). **SettingsShell** mirrors the OnboardingShell instance-swap pattern: shared Titlebar (Back→app) + rail + a content slot keyed by tab id (remount-per-tab). **Sync tab** (`SyncTab.tsx`, the first live tab, stories 37–39): background-watching on/off · poll cadence (Lively/Relaxed segmented control, only shown while polling is on) · start-at-login — optimistic-then-authoritative saves over a new IPC seam, with an inline ember "What Sync now does" note restating push+fetch-but-never-auto-apply (ADR 0006). **New env-local data owner** `sync-settings.ts` (userData, NEVER synced — ADR 0024; the testable seam this PRD reads/writes): `{ pollerEnabled, cadence: 'fast'|'relaxed', startOnLogin }`, safe defaults (poller ON · fast · autostart OFF), per-field fallback on corrupt/partial JSON (degrade, never throw; never silently more-surprising). **`sync:get/set-settings` IPC** (carry `_trace`); set-settings re-arms the side effects in index.ts: `armTrayPoller` now skips when polling is OFF + uses the chosen cadence (new RELAXED_POLL_CADENCE = 2min/15min vs fast 30s/5min), `restartTrayPoller` tears down + re-arms (+ drops the Tray when disabled so it stops pinning the process), and `app.setLoginItemSettings` applies autostart (best-effort, reconciled on each launch). Settings icon in the Workspace titlebar now opens the surface; App gains a `settings` route over the Workspace. New `Switch` ui primitive (base-ui, ember-on track). 261 tests green (+6: sync-settings round-trip/safe-fallback/never-writes-.myenv + ipc-bridge sync channels). Gate green (format · check · test). Unblocks 2-09 · 2-10 · 2-11 · 2-12 · 2-14 · 2-15 · 2-16.
2026-06-16 · CODE · ✅ Done · workflow · 3-19 electron-builder packaging with a PINNED, checksum-verified chezmoi + git bundle — made the scaffold's inert config real end-to-end. **Single source of truth**: `apps/desktop/resources/bin/tools.lock.json` pins chezmoi `2.70.5` (twpayne/chezmoi static binary per target) + git `2.53.0` (desktop/dugite-native, the portable git GitHub Desktop ships — a relocatable tree, not a bare binary, because GitTransport shells out to a real `git` that needs its own libexec/templates) with each target's release asset, URL, and **sha256**. Bump a release THERE and nowhere else → two environments on the same dotden version get byte-identical tools (reproducible per release). **`scripts/fetch-binaries.mjs`** (pure Node 24 `fetch` + host `tar`, no runtime npm dep so it runs pre-install in CI): for the host target (or `--all`/`--target`) downloads each pinned asset, **verifies sha256 (fail loud on mismatch — never ship the wrong tool silently)**, and extracts into the EXACT `src/main/foundation/tools.ts` layout — chezmoi → `bin/<platform>/<arch>/chezmoi[.exe]`, git → the whole dugite tree into `bin/<platform>/<arch>/git-dist/` (launcher `git-dist/bin/git` POSIX | `git-dist/cmd/git.exe` win). Idempotent (skips an up-to-date target; `--force` re-fetches). **Extended `tools.ts`** to probe the dugite `git-dist/` launcher path (a bare git copy can't find its support files) — backward-compatible with the existing arch/flat probes (5 prior tools.test cases still green). **electron-builder wiring**: `beforePack: scripts/before-pack.mjs` maps EB's platform/arch onto the lock key and fetches that target's binaries before `extraResources` copies `resources/bin`→`bin/` (a clean checkout ships ONLY the lock, so without the hook the installer would carry no tools); the `.yml` already produces dmg / NSIS `.exe` / AppImage **unsigned** (`mac.identity:null`) per ADR 0010 + scope-v1. The fetched binaries are gitignored (`resources/bin/.gitignore`) — only the lock + README + .gitignore are tracked. **electron-updater already a runtime `dependency`** (`^6.6.2`, ADR 0010 — packaged, not dev-only). **Proven real, not faked**: ran the host fetch → chezmoi reports `v2.70.5`, git reports `2.53.0` from the resolver's resolved paths; added a hermetic `scripts/__tests__/tools-lock.test.ts` (lock shape + every target has a sha256 + the lock's promised paths == what `resolveBundledTools` probes) and an opt-in `scripts/__tests__/fetch-binaries.smoke.test.ts` (`DOTDEN_FETCH_SMOKE=1`: real download→checksum→resolve→`--version` boot smoke, the closest hermetic stand-in for "the packaged app boots"). Updated `resources/bin/README.md` + ADR 0010 (packaging is now active, not inert; dugite-native is the bundled git). Gate green (format · check 5/5 · 255 tests + 1 network-gated skip). 3-20 still blocked by 2-16. Touches build/packaging only (commit scope `build`) — no app feature code.
2026-06-16 · CODE · ✅ Done · workflow · 3-22 CI pipeline — `.github/workflows/ci.yml` (runs on pull_request + push to main + workflow_dispatch). **The frozen install IS the ADR 0015 supply-chain gate**: `corepack enable` (ships the exact pinned pnpm@11.6.0 from root `packageManager`) → `pnpm install --frozen-lockfile`, which under pnpm 11 STILL enforces every guard configured in `pnpm-workspace.yaml` even when frozen — `strictDepBuilds` (default-true) makes a dependency whose install/build script is NOT in the committed `allowBuilds` map exit non-zero (`ERR_PNPM_IGNORED_BUILDS`); strict `minimumReleaseAge:4320` hard-fails a too-fresh transitive dep; `nodeLinker:hoisted`. So an unapproved build script or a tampered lockfile fails CI at install time, before any task runs (the workflow header documents this, ADR 0015). **Affected check/build/test**: `pnpm turbo run check:types check:lint build test --affected` — turbo runs tasks only in changed packages (default `main...HEAD`); `actions/checkout` uses `fetch-depth:0` so the merge-base/previous-commit is always present (a shallow clone would make turbo fall back to running everything). On a **push to main** the default `main...HEAD` diff is empty, so `TURBO_SCM_BASE` is pinned to `github.event.before` (the commit pushed from) — a push still validates exactly what changed; on **pull_request** turbo auto-detects `GITHUB_BASE_REF` (no override). Least-privilege `contents:read`, `concurrency` cancels redundant runs per ref. **Repo gap fixed for the gate to exist**: there was no `test` turbo task or root `test` script (so `pnpm test`/`turbo run test` errored `Missing tasks in project`); added `test` to `turbo.json` (`dependsOn:["topo"]`, mirroring check tasks) + a `"test": "turbo run test"` root script — turbo runs `@dotden/desktop#test` (vitest) and skips `@dotden/web` (no test script, NONEXISTENT). Verified: `pnpm install --frozen-lockfile` ok (script-only package.json edit doesn't touch the lockfile), `--affected` dry-run resolves the desktop task set against `HEAD~1`, CI YAML parses. Gate green (format · check 5/5 · build 2/2 · test 248 passed). Touches `.github/workflows/ci.yml` (new) + `turbo.json` + root `package.json` only — no app code (faithful to the issue's infra scope). No new ADR (faithful implementation of ADR 0015 guards + ADR 0009 monorepo).
2026-06-16 · CODE · ✅ Done · workflow · 1-13 Second-environment onboarding — new-or-returning, subscription pick, claim, real chezmoi/git↔main↔IPC↔UI↔tests. **NEW SEAM beyond 1-01/1-15** (which built only static ignore rules): a **templated `.chezmoiignore`** (`subscription-ignore.ts`, pure/Electron-free) is now the SINGLE writer of that file, folding three concerns into one generated body — the `.myenv/` rule, the static per-OS scoped-out lines (1-15), AND a chezmoi Go-template block that self-identifies via `[data].dotden_env_id` (1-05), `include`+`fromJson`s the synced registry+placements, and ignores every File whose Workspace this env does NOT subscribe to (ADR 0005). Subscription is thus decided INSIDE chezmoi at apply-time, so ONE repo materializes DIFFERENT subsets per env (spike `.scratch/poc-subscription-chezmoiignore` adapted to dotden's real `environments.json`/`workspaces.json` shape, now proven by a real-binary e2e: laptop subscribes Personal-only, desktop both → `.work-rc` lands on desktop, is `chezmoi ignored` on laptop). **Registry-entry guard both ways**: (a) ordering PRIMARY — `EnvironmentRegistry.registerWithSubscription` / `DenService.setSubscriptions` write this env's entry (default ALL Workspaces) + mirror `dotden_env_id` BEFORE any apply, and `regenerateOsScopeIgnore` re-mirrors the id before writing the template so chezmoi never sees an undefined `.dotden_env_id`; (b) template fail-safe — a MISSING entry OR missing `dotden_env_id` key (cloned-not-yet-claimed) is `hasKey`-guarded and degrades to a literal `*` ignore-EVERYTHING (apply nothing), never a chezmoi "map has no entry" error, never apply-all. **Never silent**: `DenService.subscriptionState().emptyDenWarning` surfaces WHY+fix ("this environment isn't registered yet…") so an unregistered/empty-subscription env never renders a confusing blank Den (e2e: unregistered env → empty home + warning, no error). **Un-subscribe keep/remove**: `.chezmoiignore` alone never deletes (the spike's load-bearing finding), so `unsubscribeWorkspace(ws, 'keep'|'remove')` re-compiles the ignore then either leaves the Files as untracked orphans (`keep`, safe default) or `fs.rm`s ONLY this env's local home copies (`remove` — NOT `chezmoi destroy`, which would mutate the shared source + travel; e2e proves the source `dot_work-rc` survives). Remembered default in env-local `subscription-settings.ts` (userData, never synced, fail-safe `keep` on corrupt/unknown). **DenService**: `subscriptionState`/`setSubscriptions`/`unsubscribeWorkspace`; `trackFile`/`createWorkspace`/`setFileWorkspace` now regenerate+commit the templated ignore so it TRAVELS. **EnvironmentRegistry**: `workspaces()` + `registerWithSubscription(ids?)` (the new-or-returning register, default-all). **UI** (returning-environment.md): App **LandingChooser** (new Den vs connect existing) → **ReturningShell** (rail `ReturningMenu`: Connect·Find your Den·Choose Workspaces·Review&Apply, the 4th always-upcoming handoff) → reuses onboarding **OBConnectUrl** (same paste+preflight+`chezmoi init`, ADR 0020) → **OBFoundDen** (new/returning identity claim via `env.suggestClaims`, never auto-merges) → **OBPickWorkspaces** (`SelectRow` checklist, default ALL, empty-selection warned) → `env.claim(id, ws)` (returning, keeps id/history via `claimLocalIdentity` + re-arm) or `env.registerNew(ws)` (new) → app **Review&Apply** (role b, the deliberate first materialization; a locally-existing File routes the 1-11 Conflict flow). **IPC**: `den:subscription-state`/`set-subscriptions`/`unsubscribe-workspace`/`unsubscribe-disposition`/`remember-unsubscribe-disposition` + `env:register-new`/`env:claim` (claim re-arms id-bound singletons in index.ts) + preload + contract. 248 tests green (+21: subscription-ignore renderer shape, settings store fail-safe, MyenvStore.setSubscriptions + EnvironmentRegistry register/workspaces units, ipc-bridge subscription/claim routes, 3 real-binary e2e: 2-env subsets + empty-Den fail-safe+warning + keep/remove). Gate green (format/check/test). Ties together 1-05 registry + 1-11 conflict + 1-12 automation. No new ADR (faithful implementation of ADR 0005 access boundary + ADR 0024 subscription seam, both of which already specified the templated-`.chezmoiignore` mechanism).
2026-06-16 · CODE · ✅ Done · workflow · 1-16 Offline Commit + queued pushes + retry on reconnect/next Sync, real chezmoi/git↔main↔IPC↔UI↔tests. **Offline contract (ADR 0006)**: a Commit is a LOCAL op (chezmoi re-add + `git commit`) that always succeeds with no network; only the PUSH needs connectivity, so a push that can't reach the Remote because the machine is offline is QUEUED + retried, never failing the Commit. **`offline.ts`** (pure, Electron-free, tdd'd): `isOfflineError(err)` classifies a transport failure as network-offline (queue+retry) vs server-reached rejection (surface) vs non-transport (rethrow) by matching git's REAL stderr family — `Could not resolve host` / `Failed to connect … port` / `Couldn't connect to server` / `Connection refused|timed out|reset` / `Network is unreachable` / SSH connect errors (all verified against the bundled git); a `! [rejected]` non-fast-forward / auth-denied / missing-repo is NOT offline (a blind retry can't fix it, so it must surface — never fail silently). A `CommandAbortedError` (timeout/cancel, a hung credential prompt) and a spawn-level ENOENT are explicitly NOT offline. **`PushQueue`** (durable outbox, Electron-free, tdd'd): modeled as a SINGLE persisted `pending` flag, not a per-commit list — because `git push` is all-or-nothing on the branch (sends every unpushed commit), so "what's owed" is one fact; this gives **dedup** (N offline Commits → ONE queued push, flushed in one go) + **never-drop** (the flag persists, the commits live safely in git) for free. `flush(push)`: success→clear; offline-fail→KEEP (rethrow isOffline) so the next reconnect/Sync retries; non-offline-fail→CLEAR (can't fix by retry) + rethrow; empty→no-op (never push spuriously). Corrupt/missing outbox reads as not-pending (fail safe). Environment-local (ADR 0024): outbox lives OUTSIDE the synced source tree (sibling of sourceDir by default; userData under `index.ts`). **`DenService`**: `commitTracked` Auto-sync push now catches an offline failure → `pushQueue.enqueue()` + `CommitResult.queued:true` (the Commit still records, `pushed:false`); a non-offline push error rethrows. `syncPush`/new `flushPushQueue` return `SyncPushResult {pushed,queued}` — an offline Sync QUEUES and does NOT throw (the work is safe), a successful one clears the outbox (one push flushes every unpushed commit, so Sync now IS the manual retry the issue asks for); `flushPushQueue` is the reconnect retry. New `pushPending()` for the banner. **Electron wiring** (`index.ts`): `powerMonitor` resume/unlock now `onReconnect()` → re-check Remote (1-12) AND flush queued pushes; a `net:reconnected` push tells an open window to re-read `pushPending`. **IPC**: `den:flush-push-queue` (mutates, _trace forwarded) + `den:push-pending` (read-only) + `net.onReconnected` push channel; preload + contract. **UI**: new `OfflineBanner.tsx` (the secret-and-errors spec's `Banner Offline` — `cloud` glyph, muted tone NOT destructive-red, "Offline — changes queued · Will sync when you reconnect"), shown between titlebar + body when `pushPending` (takes precedence over the incoming banner; already-fetched incoming still Applies offline — only push queues). Renderer detects connectivity via `navigator.onLine`/`online`+`offline` events → calls `den.flushPushQueue` on reconnect (complements the main-process powerMonitor path), refreshes the banner on commit/Sync/reconnect. **e2e offline trigger is deterministic + fast**: origin points at `ssh://git@127.0.0.1:1` with a short `GIT_SSH_COMMAND` ConnectTimeout (inherited via `runCommand`'s process.env merge) → a real "Connection refused/timed out" in ~1s, no flaky network. 227 tests green (+25: offline classifier units incl. real stderr signatures, PushQueue dedup/restart/retry/fail-safe, 4 real-binary DenService e2e — Auto-sync offline→queue→Sync-flush, reconnect flushPushQueue-once, survives-restart-flush-once, Manual offline-Sync-queues-not-throws → online-flush; ipc-bridge channels). Gate green (format/check/test/build). No new ADR (faithful implementation of ADR 0006 transport-not-commit + ADR 0024 env-local outbox); the full offline state-surface is 3-08.
2026-06-16 · CODE · ✅ Done · workflow · 1-15 OS Scope + inheritance (narrowable) → per-OS `.chezmoiignore`, real chezmoi↔main↔IPC↔UI↔tests. **Pure `os-scope.ts`** (Electron-free, ADR 0023) is the SOLE home of two pieces of logic, tdd'd first: (1) **inheritance is set INTERSECTION** — `narrowScope(parent, child) = intersect`, so a child can only ever be a SUBSET of its parent; "broaden past the Folder" is *unrepresentable* (the invariant lives in the math, not a guard), and `effectiveScope(chain)` folds Workspace→Group→…→File; (2) **scope→ignore translation** — `scopedOutPaths(paths, currentOs)` = exactly the paths whose effective Scope omits this OS. `null` = the universal Scope (applies everywhere, the intersection identity + the default for a freshly Tracked File — never silently scoped out); `[]` = "applies nowhere" (a real, representable floor). Property-tested (in-house mulberry32, no new dep): an effective child Scope is ALWAYS a subset of its parent; a path is scoped-out iff `scopeAppliesOn` is false. **MyenvStore** (synced `.myenv/` intent, ADR 0024): `scope` added to Workspace/Group/FilePlacement (forward-compat `null` default for pre-1-15 docs); `setFileScope`/`setGroupScope` CLAMP the request under the inherited ceiling (narrow-only); `effectiveScopeOf` resolves the chain. **ChezmoiAdapter** is now the SINGLE writer of `.chezmoiignore` — `writeOsScopeIgnore` renders via `scopedOutPaths` and ALWAYS re-emits the `.myenv/` rule first (resolving the old overwrite-vs-append conflict with `ensureIgnored`), so the synced-metadata rule survives every regeneration. **DenService**: `setFileScope`/`setGroupScope` clamp → `regenerateOsScopeIgnore` (recompiles the WHOLE Den's effective Scopes for THIS OS) → `commitMetadata` LOCALLY (ADR 0006, so the Scope travels). New `GitTransport.commitIfChanged` (no-op when a clamp leaves the model unchanged — a plain commit would fail "nothing to commit"). **Faithful-wrapper truth caught**: chezmoi treats an ignored File as UNMANAGED-here, so a scoped-out File drops out of `chezmoi managed` → `fileTree` now UNIONS `managed` with `.myenv/` placements so a scoped-out File renders **muted** (issue 1-07) instead of vanishing; `muted = in chezmoi ignored OR placed-but-unmanaged-here`. **ApplicabilityResolver** (invariant #3 owner) gains the OS-applicability clause `file.scope matches env.os` (new `out-of-scope` refusal) — a File scoped to another OS gets no `AppliesHere` witness, keeping the witness and the `.chezmoiignore` in lock-step. **IPC**: `den:set-file-scope`/`den:set-group-scope` (+ preload + contract; `Scope` exported to the renderer). **UI**: new `ScopeEditor` inspector surface (OS chips, all-on = universal, copy states narrow-never-broaden); inspector Scope row shows the effective OS set; muted rows already dim via the 1-07 `ignored` mapping. +28 tests → 202 green (os-scope unit+property, store clamp/inheritance, resolver OS-axis, ipc-bridge scope channels, 3 real-binary e2e: scope→`chezmoiignore`→`chezmoi ignored` muted + travels through Sync + clamp-not-broaden). Gate green (format/check/test/build). No new ADR (implements ADR 0003 mapping + ADR 0024 "OS Scope rules live as native `.chezmoiignore`"); CONTEXT.md "Scope" term already present.
2026-06-16 · CODE · ✅ Done · workflow · 1-12 Sync-now polish + Auto-sync + TrayPoller + OS notification + AutomationPolicy, real chezmoi/git↔main↔IPC↔UI↔tests. **AutomationPolicy** (new, ADR 0008): the risk-graded ladder gate that gates LEVELS (MVP = Manual + Auto-sync; auto-apply/yolo named to fix the ladder shape but deferred to 2-12) by **DEPENDING ON the invariant-owner types**, never re-checking — `mayAutoApply(candidate)` can only be asked with an `AutoApplyCandidate` built from real owner outputs (an `AppliesHere` witness = invariant #3, the planner's `ApplyPlanItem` carrying `blockedReason` = #2 + `requiresConfirmation` = #4), so an unsafe auto-apply is unrepresentable; `mayAutoPush` is the ONLY automatic transport (an already-Committed change), and there is deliberately NO `mayAutoCommit` (Commit is never automatic, ADR 0006). MVP `mayAutoApply` is always false → Apply stays a manual review at both rungs. **TrayPoller** (new, Electron-free, ADR 0023): the always-on detect+notify watcher, independent of Auto-sync (even Manual polls — awareness ≠ automation). Cheap `git ls-remote` latest-SHA compare (issue 1-03, provider-agnostic, no Provider API / no clone / no rate-limit cost), fetch+notify ONLY when the SHA moved; adaptive cadence (idle backoff ×2 → ceiling, activity/window-focus snaps to floor); `onReconnect` for powerMonitor wake; a read error surfaces via `onError` but NEVER crashes the loop. All IO is injected seams (scheduler/SHA-reader/notifier) so the whole loop is driven by a fake clock in unit tests. **Env-local automation store** (userData, never synced — ADR 0024; falls back to Manual on missing/corrupt/unbuilt-level, never silently more-automated). **DenService**: `commitTracked` auto-`git push`es when `AutomationPolicy.mayAutoPush()` (transport of an already-Committed change; `CommitResult.pushed` reflects it), `pollSnapshot()` (Remote URL + HEAD SHA the poller seeds with), `headSha`/`remoteUrl` git primitives. **Electron wiring** (index.ts): real `Tray` (keeps the watcher alive with the window closed — `window-all-closed` no longer quits while a tray exists), OS `Notification` on incoming, `powerMonitor` resume/unlock → onReconnect, window focus/blur → cadence. **IPC**: `automation:get/set-level` (set re-arms the DenService policy) + the one main→renderer push channel `tray-poller:incoming` (open window refreshes its banner, mirroring the OS toast). **UI**: onboarding Auto-sync opt-in now PERSISTS the level; Sync-now + Commit tooltips make transport-not-apply transparent ("Sync now pushes pending Commits + fetches incoming, then presents for review — never Applies"); the "Last commit" callout reflects auto-pushed vs local; the everyday view subscribes to the poller's incoming push. New ADR 0025 (TrayPoller detect-only + adaptive cadence). 174 tests green (+26: AutomationPolicy owner-dependency + no-auto-commit, TrayPoller detect/quiet/backoff/focus/error/reconnect, settings round-trip + safe fallback, real-binary auto-push vs Manual-local + pollSnapshot, ipc-bridge automation channels). Gate green (format/check/test). Unblocks 1-13 · 1-16 · 2-08.
2026-06-16 · CODE · ✅ Done · workflow · 1-11 Conflict resolution — ConflictModel (invariant #1 owner) + merge view + auto-merge non-overlapping, real git↔main↔IPC↔UI↔tests. **ConflictModel** is the SOLE owner of ADR 0008 invariant #1 (never auto-resolve a Conflict): a module-private brand symbol (mirroring `AppliesHere`) makes a `ResolvedConflict`'s bytes **unconstructable** except through `ConflictModel.resolve(choice)` — so "resolved bytes without a user choice" is a state the types cannot express, at ANY automation level. `ResolutionChoice = 'current'|'incoming'|'both'` maps 1:1 to Keep mine / Take theirs / Open both (and to `@pierre/diffs`' `MergeConflictResolution`). TDD'd: 7 unit tests (each side, brand present, hand-rolled object rejected, no resolve on construction, unknown-choice throws). **Mechanism = pure git, cross-env axis** (NOT `chezmoi merge`, NOT rebase): `GitTransport` gains `merge`/`conflictedPaths`(`UU`)/`conflictedFile`(`:2:` ours · `:3:` theirs · working-tree marker-union)/`writeResolved`/`completeMerge`/`abortMerge`. git auto-merges non-overlapping hunks for free → the user is never prompted for non-conflicts; only overlaps surface. **SyncEngine** gains the `conflict-resolved` event path (`routeConflictResolution`): accepts ONLY brand-verified (`isResolvedConflict`) writes, rejects any un-branded "auto-resolution" — the load-bearing per-event-path test (example accept/reject + property: no accepted write is ever un-branded). **DenService**: `detectConflicts` (fetch+merge → auto-merged flag or 3-sided Conflicts), `resolveConflictFile` (rebuilds the model from LIVE git sides — never trusts client bytes — mints via ConflictModel, routes through SyncEngine, writes+stages), `completeConflictResolution` (`git commit`; git refuses while `UU` remains), `abortConflictResolution`. **IPC/preload/contract**: `den:detect-conflicts`/`resolve-conflict`/`complete-conflicts`/`abort-conflicts`. **UI**: `ConflictResolver.tsx` (conflict-resolver screen spec) — ConflictFiles list · `@pierre/diffs` `UnresolvedFile` merge view set READ-ONLY (`mergeConflictActionsType:'none'` → the lib's own `resolveConflict()` is never reachable) · Keep mine/Take theirs/Open both buttons → `den.resolveConflict` → ConflictModel · n/m progress · Apply resolution (Secondary→ember Primary when all resolved) · Abort (confirmed). Workspace shows a ⚠ Resolve-conflicts callout when the Remote axis has conflicts. **Spike proven** by 3 real-binary e2e (non-overlapping auto-merges with no prompt; overlapping surfaces 3 sides + resolve writes ONLY chosen bytes + merge commits clean; Abort resolves nothing). 148 tests green (+14). Gate green (format/check/test/build). Unblocks part of 1-13 (still waits on 1-12).
2026-06-16 · CODE · ✅ Done · workflow · 1-10 review fix (invariant #4 was NOT enforced on Apply): `DenService.applyIncoming` derived deletion-ness SOLELY from `confirmedDeletions`, so an UNCONFIRMED incoming deletion (source removed the File, `chezmoi status` ` D <path>`) misclassified as `incoming-clean`/create, skipped the dead `kind==='delete'` gate, reached `applyGuarded` and `chezmoi apply <path>` **deleted the destination file** — reproduced against bundled chezmoi v2.70.5 with a `.chezmoiremove` directive. **Fix**: `applyIncoming` now reads ONE `chezmoi status` snapshot for both invariants — col X drives the invariant-#2 local-drift set, col Y=D (`parseIncomingDeletions`) classifies REAL incoming deletions, routed `incoming-delete` regardless of confirmation so the planner marks them `kind:'delete'`/`requiresConfirmation` and the `kind==='delete' && !confirmed` gate is now LIVE (refuses `needs-confirmation`, never reaching `applyGuarded`). **UI seams** (the dead `requiresConfirmation` plumbing): ReviewApply Apply one/all and the Workspace row-Apply now GATE any deletion behind a Destructive `ConfirmDialog` that names the file, then pass it in `den.apply(paths, confirmedDeletions)`. **Test**: replaced the vacuous invariant-#4 e2e (which never made a real deletion and asserted the unconfirmed File was APPLIED) with a REAL `.chezmoiremove` incoming deletion across two envs — asserts unconfirmed=refused+file-survives, confirmed=file-removed; verified it FAILS against the buggy classification. Gate green (format/check/134 tests). Commit amended.
2026-06-16 · CODE · ✅ Done · workflow · 1-10 ApplyPlanner invariants — uncommitted-edit guard + incoming-deletion confirmation: made `ApplyPlanner` the SOLE owner of ADR 0008's invariants #2 (never lose data silently) + #4 (confirm incoming deletions), with `SyncEngine`/the Apply surface/`ChezmoiAdapter`'s caller all CONSUMING its verdict, never re-checking (ADR 0008 review discipline). **Planner**: `planIncoming(changes, localEdits)` — each item carries `blockedReason:'uncommitted-edit'` when the File is in the local-drift set (data-driven, no per-kind special case) + `requiresConfirmation:true` for every `delete`; `ready`/`blocked`/`deletions` splits so no caller re-classifies; `planIncomingClean` kept (built on `planIncoming`) for the MVP create path. **Local-drift axis (invariant #2)**: the **authoritative atomic re-check** lives in `ChezmoiAdapter.applyGuarded` (status-then-write inside one method → NO plan-time→apply-time TOCTOU), throwing `UncommittedLocalEditError`; `ChezmoiAdapter.localEdits()` reads `chezmoi status` **column X** (the local hand-edit) via `parseChezmoiStatus`. **SyncEngine**: gains the `incoming-delete` event path (`STATUS_TO_KIND` create/delete) + a `localEdits` passthrough to the planner — it never decides blocking/confirmation itself. **DenService**: `applyIncoming(paths, trace, confirmedDeletions)` snapshots `localEdits` for the plan-time block, applies each File via the guarded write (so even a File dirtied AFTER review is refused), surfaces `ApplyRefusal` (`blocked-uncommitted-edit`/`needs-confirmation`/`not-applicable`) per File — a deletion applies ONLY if explicitly confirmed; `computeIncoming` surfaces incoming deletions (`chezmoi status` col Y=D via new `parseIncomingDeletions`); `IncomingReviewItem` now carries `kind`+`requiresConfirmation`. **IPC**: `den:apply` + preload + contract gain `confirmedDeletions`. TDD'd exhaustively: planner units + property (every dirty File blocked, every deletion confirm-required), SyncEngine delete/block examples + extended property over create/delete/conflict×drift, `parseIncomingDeletions` units, 3 real-binary e2e (guard blocks-with-warning leaving the local edit INTACT; atomic write-path re-check refuses a post-review drift = no TOCTOU; confirmed-vs-unconfirmed deletion). 134 tests green (+20). Gate green (format/check/test). No new ADR (implements ADR 0008). Unblocks nothing new (1-11/1-12 already grabbable; 2-12/2-13 still gated on others).
2026-06-16 · CODE · ✅ Done · workflow · 1-09 Remote-axis decorations + Review & Apply surface (one/all, atomicity, retry): the SECOND status axis + the reviewed-Apply surface, real chezmoi↔main↔IPC↔UI↔tests. **Remote axis**: a pure `remoteAxisDecoration` maps a File's marker→overlay glyph (`incoming`→↓, `conflict`→⚠), driven through `@pierre/trees` `renderRowDecoration` so the glyph lands directly LEFT of the local M/A/D/R/U letter (the 1-00 spike geometry, no `unsafeCSS`). The everyday tree fetches `den:incoming-summary` on load/after-Sync to populate the per-File Remote-axis map. **Top-level entry**: a `Tone=Incoming` `IncomingBanner` strip ("N incoming from `<env>`") inserted between titlebar + body (detach+insert, not overlay — sync-states spec; collapsed middle grid row when nothing incoming) whose **Review & Apply** CTA + the inspector incoming-card CTA navigate to the surface. **Review & Apply surface** (`ReviewApply.tsx`, modeled on Returning·Review&Apply): left list grouped CONFLICTS (⚠, surfaced-not-applied — ConflictModel owns them, 1-11) / APPLIES CLEANLY (↓); center shows the incoming **diff before any Apply** (`den:incoming-diff` → `chezmoi diff` → `@pierre/diffs`); **Apply one** (selected) / **Apply all** (every clean File). **Per-file atomicity** (the load-bearing logic, TDD'd): `DenService.applyIncoming` rewritten to a SEPARATE `chezmoi apply <file>` per File so one failure never blocks the rest; `ApplyResult` now carries per-File `results`/`applied`/`failed`; each failure reports a reason + `retryable`, and **Retry re-runs ONLY the failed paths**. A witness-refused path is surfaced as a non-retryable failure (never silently dropped). Source-env naming derives the "from `<env>`" label from the registry (non-self env, neutral fallback). New IPC `den:incoming-summary` (sync, _trace forwarded) + `den:incoming-diff` (read-only); preload + contract (`IncomingSummary`/`RemoteAxisMarker`/`ApplyFileResult`). Tests +8 → 114 green: 3 per-file atomicity (isolation / retry-only-failures / non-applicable-surfaced, real chezmoi w/ a stubbed per-File apply), 2 e2e (incoming-summary names source + marks each File ↓, incoming-diff previews before Apply; neutral-source fallback), 3 remote-axis mapping (↓/⚠/null), + IpcBridge + updated existing listIncoming/apply assertions. Gate green (format/check/test/build). Unblocks 1-10 (ApplyPlanner guards) · 1-11 (Conflict resolution) · 1-12 (Sync automation/tray).
2026-06-15 · CODE · ✅ Done · workflow · 1-14 Workspaces (access boundary, invisible until 2nd) + nested Groups: extended the synced `.myenv/` Workspace model (ADR 0024) — `Workspace` gains a flat `parentId`-linked nested `Group` tree, `FilePlacement` gains `groupId`. New `MyenvStore` ops: `createWorkspace` (mints a 2nd+ access boundary), `createGroup` (nested, refuses cross-Workspace parent), `moveFileToGroup` (the organize-ONLY move) and `setFileWorkspace` (access move, resets Group). The load-bearing invariant (ADR 0005): a Group move changes NEITHER access (`workspaceId`) NOR on-disk path (`targetPath`) — proven byte-for-byte in unit + e2e tests; `setFileWorkspace` is the contrast (access DOES change). `placeFile` now preserves a File's Group on re-Track (sticky organization); `readWorkspaces` normalizes legacy docs (no `groups`/`groupId`) so older Dens load forward-compat. New `DenService` methods commit the `.myenv/`-only edit LOCALLY (ADR 0006) so the tree travels on Sync (proven by an env-B clone test reconstructing the nested Group tree + Workspaces). `OperationKind` gains `organize`. IPC: `den:create-workspace`/`den:create-group`/`den:move-to-group`/`den:set-file-workspace` (each MUTATES so its `_trace` is forwarded), preload + contract. Renderer: new `WorkspaceSidebar` renders Workspace sections + the nested Group tree with add-Workspace/add-Group affordances, shown ONLY once a 2nd Workspace OR any Group exists (the "invisible until 2nd" rule) — until then the flat `@pierre/trees` tree stands and the concept stays hidden; `FileRow` renders grouped File rows (real status letters, `data-item-path` so the 1-08 right-click verbs still work); inspector Group selector files the selected File. 106 tests green (+11). Unblocks 1-15 (OS Scope).
2026-06-15 · CODE · ✅ Done · workflow · 1-08 right-click row verbs + Untrack + Delete everywhere: row context menu (`@base-ui` ContextMenu over the `@pierre/trees` tree; right-clicked File recovered from the row's `data-item-path`) offers Commit · Apply · Untrack · Delete everywhere — the destructive verb visibly distinct (separator above + red text/badge). Untrack→`chezmoi forget` (source + `.myenv/` placement dropped, File STAYS on disk on EVERY environment; Default-tone `ConfirmDialog` whose copy says so — non-destructive, NOT styled red). Delete everywhere→`chezmoi destroy --force` (source + destination removed; Destructive-tone confirm that NAMES every affected environment via new `den:affected-environments` blast-radius query = envs subscribed to the File's Workspace, self first). Both commit LOCALLY (ADR 0006). `den:untrack`/`den:delete-everywhere`/`den:affected-environments` IPC + preload + Workspace wiring; `OperationKind` gains untrack/delete-everywhere; `MyenvStore.removePlacement`; `button.tsx` destructive/outline variants. **Caught + fixed a real bug while TDD-ing the seam**: forget/destroy *delete* the source-state file (`dot_zshrc`) but `untrackFile`/`deleteEverywhereFile` committed only `['.myenv','.chezmoiignore']`, leaving the deletion unstaged → the removed File stayed committed in the Remote and would re-appear on the next Sync. Switched both to `git.commitAll` so the deletion travels; proven by a cross-env clone test (env B clones a Den without the destroyed File). +4 DenService e2e tests (untrack keeps-on-disk, destroy removes-everywhere, deletion-travels, affectedEnvironments blast radius). 95 tests green. Unblocks nothing new (1-09 already grabbable).
2026-06-15 · CODE · ✅ Done · workflow · 1-07 fix (reviewer): the local status axis read the WRONG `chezmoi status` column. Per `chezmoi help status` (verified empirically against the bundled binary), column 1/X = last-written-vs-actual = the LOCAL edit; column 2/Y = actual-vs-target = what `apply` will do = the INCOMING axis (1-09). `parseChezmoiStatus` preferred Y, so a local delete (`DA`) mislabeled "added" and incoming-only rows (` M`/` A`, no local edit) leaked onto the local axis. Now reads **column X only** (no Y fallback): `DA`→deleted, ` M`/` A`/` R`→omitted (incoming, 1-09's job), `MM`→modified (unchanged). Rewrote the doc comment + unit-test spec to match chezmoi's documented columns; e2e fileTree (local `MM`→modified) still green. 91 tests green. Unblocks 1-09's Remote axis cleanly (no incoming collision on the local axis). Commit amended.
2026-06-15 · CODE · ✅ Done · workflow · 1-07 three-pane view: real chezmoi↔main↔IPC↔UI three-pane. NEW `den:tree`/`den:diff` IPC (read-only, _trace-asserted) over `DenService.fileTree` (managed Files + `parseChezmoiStatus` local axis M/A/D/R/U + `chezmoi ignored`→muted + `.myenv/` placement, all in one snapshot) and `fileDiff` (`chezmoi diff <file>`). New `ChezmoiAdapter.managed()`/`ignoredPaths()` + pure `chezmoi-status.ts` parser. Renderer Workspace rebuilt: @pierre/trees FileTree driven by setGitStatus + working search (⌘K) + inline rename + drag-reorganize + a no-op renderRowDecoration seam for the 1-09 Remote axis; @pierre/diffs PatchDiff of the selected File's real diff; inspector FILE info (Workspace/Scope/Path/Status) + incoming-callout seam (1-09). @pierre/* betas already pinned; pnpm-patched @pierre/trees to kill the `Math.random()` sticky-inset jitter; Shiki trimmed to dotden's config languages via a grammar-free `shiki` alias-shim (renderer build 308→93 chunks; wolfram/emacs-lisp/cpp/etc gone). Test hygiene: force `commit.gpgsign=false` in the temp-repo fixtures so host 1Password signing can't hang `git commit`. 90 tests green (8 status-parser + 1 fileTree/fileDiff e2e + tree/diff IpcBridge). Unblocks 1-08/1-09/1-14/2-01.
2026-06-15 · 1-07 · 🟡 In progress · workflow · Started three-pane view slice.
2026-06-15 · CODE · ✅ Done · workflow · 1-06 tool-catalog discovery scan + first-environment onboarding: DiscoveryScanner scans the home dir grounded in a known-tools catalog (feature-detection, not a blind sweep — ADR 0022) + drag-in/browse `inspectCustomPath` for missed Files (home-relative, escape-guarded); `discover:scan`/`discover:inspect-path` IPC (carry `_trace`, route to scanner). UI: OnboardingShell (rail + content slot + step router) + OnboardingMenu (6-step rail) + OBContent steps Welcome→CreateRepo→ConnectURL(reuses 1-03 preflight/connect)→Discover→Commit(Commit+Sync via 1-04)→AutoSync(wired opt-in slot, engine=1-12)→Done; ListRow scan rows; picks Track via 1-04 `den.track` → default Workspace auto-seeded (no org asked); App.tsx routes onboarding↔Workspace. 81 tests green (11 DiscoveryScanner + discover IPC). 2-07/2-08 dep satisfied (still need their other blockers).
2026-06-15 · CODE · ✅ Done · workflow · 1-05 environment registry & identity: stable hostname-independent id minted at setup + frozen setup-hostname claim hint; editable label (one-line registry diff, no churn, id untouched); attribution derived live from `git log` (never persisted); own id mirrored to local chezmoi `[data].dotden_env_id` (spike-proven subscription-`.chezmoiignore` seam); new-or-returning probe (readLocalIdentity/claimLocalIdentity) + suggestClaims by OS+setup-hostname (no auto-merge); `env:*` IPC + editable EnvironmentBadge UI; ADR 0024 extended. 69 tests green.
2026-06-15 · CODE · ✅ Done · workflow · 1-04 first end-to-end thread (Track→Commit→Sync→2nd-env Apply, incoming-clean) real across two envs; IpcBridge/_trace + OperationTracer + SyncEngine + AppliesHere witness + ApplyPlanner + .myenv/ store + 3-pane shell (@pierre/trees+diffs); 51 tests green (SyncEngine property + real-binary e2e). 1-05/1-06/1-07/3-01 unblocked.
2026-06-15 · 1-04 · 🟡 In progress · workflow · Started end-to-end thread slice.
2026-06-15 · 1-03 · ✅ Done · pi · RemoteClient preflight/init/latest-SHA slice landed with typed IPC/UI and tests; 1-04 now grabbable.
2026-06-15 · 1-03 · 🟡 In progress · pi · Started RemoteClient connect/preflight/init/latest-SHA slice.
2026-06-15 · tracker · ✅ Updated · pi · Reconciled Ready now after 1-01 completion and V1-Lean 1-02 deferral; 1-03 is now grabbable.
2026-06-15 · 1-01 · ✅ Done · pi · Vitest harness + faithful chezmoi/git wrapper foundation landed; tests/typecheck/lint/build pass.
2026-06-15 · 1-01 · 🟡 In progress · pi · Started Vitest harness + ChezmoiAdapter/GitTransport foundation.
